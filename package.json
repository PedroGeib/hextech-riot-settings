{
  "name": "hextech-riot-settings",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop app for managing League of Legends and TFT configuration files.",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "vendor": "node -e \"require('fs').copyFileSync('node_modules/js-yaml/dist/js-yaml.min.js', 'renderer/vendor/js-yaml.min.js')\""
  },
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
